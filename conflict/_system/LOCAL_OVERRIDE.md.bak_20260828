---

# 本機執行的覆寫規則（優先於上文）

本次為**本機草稿產製**，不是發布。上文的發布流程**全部不執行**。

1. **寫到 `conflict/_drafts/Brief_{{DATE}}.md`**，不要寫進 `conflict/briefs/`。
2. **不要執行 `rebuild-manifest.py`。**
3. **不要 git add、不要 commit、不要 push。** 這份稿子要先給人看過才會發布。
4. 歷史比對照舊——讀 `conflict/briefs/` 內既有晨報（那是已發布的）；
   另外也看一下 `conflict/_drafts/` 內有無尚未發布的近日草稿，避免與未發布稿重複。
5. 今天的日期是 **{{DATE}}**（台灣時間），直接用，不必再自行推算。

其餘規範一律照上文執行，**不因為是草稿而放寬**。

草稿會由人過目後，以 `python conflict/_system/publish.py {{DATE}}` 發布。
該腳本會檢查固定欄位、來源等級、待查核、佔位字樣與文體，
所以這些項目請一次做對，不要留給發布時再修。
