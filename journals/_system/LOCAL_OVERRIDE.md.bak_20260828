---

# 本機執行的覆寫規則（優先於上文）

本次為**本機草稿產製**，不是發布。上文的發布流程**全部不執行**。

1. **寫到 `journals/_drafts/Brief_{{DATE}}.md`**，不要寫進 `journals/briefs/`。
2. **不要執行 `rebuild-manifest.py`。**
3. **不要 git add、不要 commit、不要 push。** 這份稿子要先給人看過才會發布。
4. 書目資料在 `journals/_drafts/crossref_{{DATE}}.json`，已由 `fetch.py` 產出，直接讀。
5. 歷史比對照舊——讀 `journals/briefs/` 內既有晨報；
   另外也看一下 `journals/_drafts/` 內有無尚未發布的近日草稿。
6. 今天的日期是 **{{DATE}}**（台灣時間），直接用，不必再自行推算。

其餘規範一律照上文執行，**不因為是草稿而放寬**。

草稿會由人過目後，以 `python journals/_system/publish.py {{DATE}}` 發布。
